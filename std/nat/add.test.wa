import { Add, Add1, Nat, Zero } from "add.wa"

trace steps 5 {
  Add1 [Nat::add1(Nat::zero), _x]
}

// find x {
//   Add [x, x, Nat::zero]
// }

// find x {
//   Add [x, x, Nat::add1(Nat::add1(Nat::zero))]
// }
