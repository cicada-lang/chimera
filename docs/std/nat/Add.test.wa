import { Add, Add1, Nat, Zero } from "Add.wa"

// trace steps 5 {
//   Add1(add1(zero()), _x)
// }

find x {
  Add(x, x, zero())
}

find x {
  Add(x, x, add1(add1(zero())))
}

// The following query diverges.

// trace steps 5 {
//   Add(x, y, z)
//   Add(x, y, add1(z))
// }
