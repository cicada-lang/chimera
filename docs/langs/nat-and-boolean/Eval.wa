import { Value } from "Exp.wa"
import { Step } from "Step.wa"

Eval [v, v]
------------ {
  Value v
}

Eval [e, v]
-------------- {
  Step [e, r]
  Eval [r, v]
}

find [e, v] {
  e = Exp::if(
    Exp::isZero(Exp::add1(Exp::add1(Nat::zero))),
    Exp::add1(Nat::zero),
    Exp::add1(Exp::add1(Nat::zero)),
  )

  Eval [e, v]
}
