import { Exp } from "Exp.wa"

Env Env::null

Env Env::cons(name, exp, rest)
------------------------------- {
  Exp exp
  Env rest
}

find env {
  env = Env::cons("id", Exp::fn("x", Exp::var("x")), Env::null)
  Env env
}
